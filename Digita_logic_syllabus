// here multiplexer is divec that take 2powern input and produce that 1 output and n is the no of selectlines
// here demux is opposite of it and it take single input and produce multiple output
// Decoder is device that convert n input into 2^n output and so on
